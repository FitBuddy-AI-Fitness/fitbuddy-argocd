apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: fitbuddy-user-service
spec:
  type: ClusterIP
  selector:
    app: fitbuddy-user-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
