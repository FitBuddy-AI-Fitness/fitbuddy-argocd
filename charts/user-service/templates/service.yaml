apiVersion: v1
kind: Service
metadata:
  name: fitbuddy-user-service
  labels:
    app: fitbuddy-user-service
spec:
  type: ClusterIP
  selector:
    app: fitbuddy-user-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.service.port }}
