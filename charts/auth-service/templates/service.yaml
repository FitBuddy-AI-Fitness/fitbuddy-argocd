apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: fitbuddy-auth-service
spec:
  type: ClusterIP
  selector:
    app: fitbuddy-auth-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
