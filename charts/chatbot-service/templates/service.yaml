apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  labels:
    app: fitbuddy-chatbot-service
spec:
  type: ClusterIP
  selector:
    app: fitbuddy-chatbot-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
