apiVersion: v1
kind: ServiceAccount
metadata:
  name: fitbuddy-sa-workout-service
  {{- if .Values.workloadIdentity.enabled }}
  annotations:
    azure.workload.identity/client-id: {{ .Values.workloadIdentity.clientId | quote }}
  labels:
    azure.workload.identity/use: "true"
  {{- end }}
