apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitbuddy-workout-service
  labels:
    app: fitbuddy-workout-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: fitbuddy-workout-service
  template:
    metadata:
      labels:
        app: fitbuddy-workout-service
        {{- if and .Values.workloadIdentity .Values.workloadIdentity.enabled }}
        azure.workload.identity/use: "true"
        {{- end }}
    spec:
      serviceAccountName: fitbuddy-sa
      containers:
        - name: workout-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.service.port }}
          resources:
{{ toYaml .Values.resources | indent 12 }}
          envFrom:
            - configMapRef:
                name: fitbuddy-config-workout-service
            - secretRef:
                name: bodygpt-secrets
          startupProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.service.port }}
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.service.port }}
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 5


