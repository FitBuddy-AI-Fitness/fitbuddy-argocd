apiVersion: v1
kind: Service
metadata:
  name: fitbuddy-yolo-exercise-service
  labels:
    app: fitbuddy-yolo-exercise-service
spec:
  selector:
    app: fitbuddy-yolo-exercise-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.service.port }}
