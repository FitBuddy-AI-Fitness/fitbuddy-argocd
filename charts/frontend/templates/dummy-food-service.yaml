apiVersion: v1
kind: Service
metadata:
  name: food-recognition-service
  labels:
    app: dummy-food-service
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
